@{
    var appName = "SPESH AGRO";
    var userType = Context.Session.GetString("UserType");
    var userName = Context.Session.GetString("UserName");

    // ✅ support both names used in your project
    var isUser = userType == "User" || userType == "Customer";
    var isAdmin = userType == "Admin";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - @appName</title>

    <!-- Bootstrap(CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Animations -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />

    <!-- Tabulator -->
    <link href="https://unpkg.com/tabulator-tables@5.5.0/dist/css/tabulator.min.css" rel="stylesheet">

    <!-- Select2 -->
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />

    <!-- Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

    <!-- AOS Animation Library -->
    <link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet">

    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
</head>

<!-- AOS Animation Script -->
<script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>

<body class="d-flex flex-column min-vh-100">

    <!-- NAVBAR STYLE -->
    <style>
        /* 3 equal sections on lg+ */
        @@media (min-width: 992px) {
            .nav-grid {
                display: grid;
                grid-template-columns: 1fr 1fr 1fr; /* equal space */
                align-items: center;
            }

            .nav-left {
                justify-self: start;
            }

            .nav-center {
                justify-self: center;
            }

            .nav-right {
                justify-self: end;
            }

            .nav-toggle {
                display: none;
            }
            /* hide toggler on lg+ */
        }
    </style>

    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top nav-glass shadow-sm">
        <div class="container py-1 nav-grid">

            <!-- LEFT: Brand -->
            <div class="nav-left">
                <a class="navbar-brand d-flex align-items-center gap-2" href="@Url.Action("Index", "Home")">
                    <span class="brand-logo">
                        <img src="~/Images/Spesh.png" alt="Logo" />
                    </span>
                    <span class="brand-text">@appName</span>
                </a>
            </div>

            <!-- MOBILE RIGHT COLUMN: Hamburger + Auth (stacked) -->
            <div class="ms-auto d-lg-none d-flex flex-column align-items-end">

                <!-- Toggler (top) -->
                <button class="navbar-toggler border-0 nav-toggle" type="button"
                        data-bs-toggle="collapse" data-bs-target="#mainNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Mobile Auth (under hamburger) -->
                <div class="mt-2 d-flex flex-column align-items-end gap-2">
                    @if (userType == null)
                    {
                        <a asp-controller="Auth" asp-action="Login"
                           class="btn btn-light btn-sm px-3 fw-semibold w-100 text-center">Login</a>

                        <a asp-controller="Auth" asp-action="Register"
                           class="btn btn-warning btn-sm px-3 fw-semibold w-100 text-center">Register</a>
                    }
                    else if (isUser)
                    {
                        <a asp-controller="Profile" asp-action="Index"
                           class="btn btn-outline-light btn-sm px-3 fw-semibold w-100 text-center">
                            Edit Profile
                        </a>

                        <a asp-controller="Auth" asp-action="Logout"
                           class="btn btn-danger btn-sm px-3 fw-semibold w-100 text-center">Logout</a>
                    }
                    else if (isAdmin)
                    {
                        <a asp-controller="Admin" asp-action="Dashboard"
                           class="btn btn-outline-light btn-sm px-3 fw-semibold w-100 text-center">Dashboard</a>

                        <a asp-controller="Admin" asp-action="Products"
                           class="btn btn-outline-warning btn-sm px-3 fw-semibold w-100 text-center">Products</a>

                        <a asp-controller="Auth" asp-action="Logout"
                           class="btn btn-danger btn-sm px-3 fw-semibold w-100 text-center">Logout</a>
                    }
                    else
                    {
                        <a asp-controller="Auth" asp-action="Logout"
                           class="btn btn-danger btn-sm px-3 fw-semibold w-100 text-center">Logout</a>
                    }
                </div>
            </div>

            <!-- CENTER: Links -->
            <div id="mainNav" class="collapse navbar-collapse justify-content-center nav-center">
                <ul class="navbar-nav nav-pills gap-lg-2 gap-1 mt-3 mt-lg-0">
                    <li class="nav-item">
                        <a class="nav-link nav-link-pill" href="@Url.Action("Index", "Home")">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link nav-link-pill" href="#customer-reviews">Review</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link nav-link-pill nav-shop-glow"
                           asp-controller="Shop"
                           asp-action="Index">
                            Shop
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link nav-link-pill" href="/About">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link nav-link-pill" href="#site-footer">Contact</a>
                    </li>

                    
                </ul>
            </div>

            <!-- RIGHT: Auth/User (Desktop only) -->
            <div class="nav-right ms-lg-auto mt-3 mt-lg-0 d-none d-lg-flex">
                @if (userType == null)
                {
                    <div class="d-flex gap-2">
                        <a asp-controller="Auth" asp-action="Login" class="btn btn-light btn-sm px-3 fw-semibold">Login</a>
                        <a asp-controller="Auth" asp-action="Register" class="btn btn-warning btn-sm px-3 fw-semibold">Register</a>
                    </div>
                }
                else if (isUser)
                {
                    <ul class="navbar-nav me-2">
                        <li class="nav-item">
                            <a class="nav-link nav-link-pill" asp-controller="Profile" asp-action="Index">
                                Edit Profile
                            </a>
                        </li>
                    </ul>

                    <div class="user-chip me-2">
                        <img src="https://cdn-icons-png.flaticon.com/512/149/149071.png" alt="User" />
                        <span>@userName (@userType)</span>
                    </div>

                    <a asp-controller="Auth" asp-action="Logout" class="btn btn-danger btn-sm px-3 fw-semibold">Logout</a>
                }
                else if (isAdmin)
                {
                    <ul class="navbar-nav me-2">
                        <li class="nav-item">
                            <a class="nav-link nav-link-pill" asp-controller="Admin" asp-action="Dashboard">Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link nav-link-pill" asp-controller="Admin" asp-action="Products">Products</a>
                        </li>
                    </ul>

                    <div class="user-chip me-2">
                        <img src="https://cdn-icons-png.flaticon.com/512/149/149071.png" alt="User" />
                        <span>@userName</span>
                    </div>

                    <a asp-controller="Auth" asp-action="Logout" class="btn btn-danger btn-sm px-3 fw-semibold">Logout</a>
                }
                else
                {
                    <div class="user-chip me-2">
                        <img src="https://cdn-icons-png.flaticon.com/512/149/149071.png" alt="User" />
                        <span>@userName (@userType)</span>
                    </div>

                    <a asp-controller="Auth" asp-action="Logout" class="btn btn-danger btn-sm px-3 fw-semibold">Logout</a>
                }
            </div>

        </div>
    </nav>

    <style>
        /* ===== Navbar Base ===== */
        .nav-glass {
            background: linear-gradient(90deg, #0f5132, #198754, #0f5132);
            backdrop-filter: blur(8px);
            padding-top: .6rem;
            padding-bottom: .6rem;
        }

        .brand-logo {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: #fff;
            padding: 3px;
            display: grid;
            place-items: center;
            box-shadow: 0 2px 8px rgba(0,0,0,.2);
        }

            .brand-logo img {
                width: 100%;
                height: 100%;
                border-radius: 50%;
                object-fit: cover;
            }

        .brand-text {
            font-family: "Poppins", "Segoe UI", sans-serif;
            font-weight: 800;
            letter-spacing: .8px;
            font-size: 1.35rem;
            color: #fff;
        }

        .nav-link-pill {
            color: rgba(255,255,255,.9) !important;
            padding: .45rem .9rem !important;
            border-radius: 999px;
            transition: .2s ease;
            font-weight: 600;
        }

            .nav-link-pill:hover {
                background: rgba(255,255,255,.15);
                transform: translateY(-1px);
            }

        /* remove yellow active bg completely */
        .nav-pills .nav-link.active {
            background: transparent !important;
            color: rgba(255,255,255,.95) !important;
            box-shadow: none !important;
        }

        /* ===== SHOP: static black border + rotating white glow trace ===== */
        .nav-shop-glow {
            position: relative;
            border-radius: 999px;
            background: transparent;
            border: 2px solid #000;
            z-index: 0;
            overflow: visible;
        }

        @@media (max-width: 991.98px) {
            .navbar-nav .nav-shop-glow {
                display: inline-flex !important;
                width: max-content !important;
                align-self: center;
                margin-left: auto;
                margin-right: auto;
            }
        }

        .nav-shop-glow::before {
            content: "";
            position: absolute;
            inset: -2px;
            border-radius: 999px;
            padding: 2px;
            background: conic-gradient(from 0deg, transparent 0deg, transparent 320deg, rgba(255,255,255,0.1) 330deg, rgba(255,255,255,1) 345deg, rgba(255,255,255,0.1) 360deg);
            -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            animation: shopTrace 2.0s linear infinite;
            filter: blur(0.6px) drop-shadow(0 0 6px rgba(255,255,255,0.95));
            pointer-events: none;
            z-index: 2;
        }

        @@keyframes shopTrace {
            to {
                transform: rotate(360deg);
            }
        }

        .nav-shop-glow:hover {
            background: rgba(255,255,255,.12);
        }

        /* ===== user chip ===== */
        .user-chip {
            display: flex;
            align-items: center;
            gap: .5rem;
            background: rgba(255,255,255,.15);
            padding: .35rem .6rem;
            border-radius: 999px;
            color: #fff;
            font-weight: 600;
        }

            .user-chip img {
                width: 30px;
                height: 30px;
                border-radius: 50%;
                object-fit: cover;
                border: 2px solid rgba(255,255,255,.7);
            }
    </style>

    <!-- MAIN CONTENT -->
    <main class="flex-grow-1">
        @RenderBody()
    </main>

    <!-- ===== Footer (Mint/Emerald theme) ===== -->
    <style>
        html {
            scroll-behavior: smooth;
        }

        #site-footer {
            position: relative;
            background: linear-gradient(180deg, rgba(236,253,245,.92) 0%, rgba(255,255,255,.98) 55%), #f0fff4;
            border-top: 1px solid rgba(16,185,129,.18);
            color: #062e1a;
        }

            #site-footer .footer-title {
                font-weight: 800;
                font-size: 1.05rem;
                margin-bottom: .9rem;
                color: #065f46;
            }

            #site-footer .footer-link {
                color: #0f5132;
                text-decoration: none;
                display: inline-flex;
                align-items: center;
                gap: .45rem;
                padding: .25rem 0;
            }

                #site-footer .footer-link:hover {
                    color: #046c4e;
                    text-decoration: underline;
                }

            #site-footer .ct-pill {
                display: inline-flex;
                align-items: center;
                gap: .5rem;
                padding: .4rem .7rem;
                border-radius: 999px;
                background: rgba(16,185,129,.12);
                border: 1px solid rgba(16,185,129,.22);
                color: #065f46;
                font-weight: 600;
            }

            #site-footer .nl-wrap {
                display: flex;
                gap: .5rem;
            }

            #site-footer .nl-input {
                border: 1px solid rgba(16,185,129,.22);
                background: #fff;
                border-radius: 10px;
                padding: .6rem .8rem;
                flex: 1;
                color: #062e1a;
            }

                #site-footer .nl-input::placeholder {
                    color: #6b8f82;
                }

            #site-footer .nl-btn {
                border: none;
                border-radius: 10px;
                padding: .6rem 1rem;
                background: linear-gradient(90deg, #16a34a, #0ea5a4);
                color: #fff;
                font-weight: 700;
                box-shadow: 0 8px 18px rgba(14,165,164,.25);
            }

                #site-footer .nl-btn:hover {
                    filter: saturate(1.08);
                }

            #site-footer .social a {
                width: 38px;
                height: 38px;
                display: inline-grid;
                place-items: center;
                border-radius: 999px;
                margin-right: .4rem;
                background: rgba(16,185,129,.12);
                border: 1px solid rgba(16,185,129,.22);
                color: #065f46;
            }

                #site-footer .social a:hover {
                    background: rgba(16,185,129,.18);
                }

            #site-footer .foot-bottom {
                border-top: 1px solid rgba(16,185,129,.18);
                color: #0f5132;
            }
    </style>

    <footer id="site-footer" class="mt-5">
        <div class="container py-5">
            <div class="row g-4">
                <div class="col-12 col-lg-4">
                    <h5 class="footer-title">SPESH AGRO</h5>
                    <p class="mb-3" style="max-width: 420px;">
                        Fresh, lab-tested dairy & meat — delivered with care. Sustainably produced, transparently priced.
                    </p>

                    <div class="d-flex flex-wrap gap-2 mb-2">
                        <span class="ct-pill"><i class="bi bi-telephone"></i> +880 1XXX-XXXXXX</span>
                        <span class="ct-pill"><i class="bi bi-envelope"></i> hello@speshagro.com</span>
                    </div>
                    <div class="d-flex flex-wrap gap-2">
                        <span class="ct-pill"><i class="bi bi-geo-alt"></i> Chittagong, Bangladesh</span>
                        <span class="ct-pill"><i class="bi bi-clock"></i> 8:00–22:00</span>
                    </div>
                </div>

                <div class="col-6 col-lg-2">
                    <h6 class="footer-title">Shop</h6>
                    <ul class="list-unstyled mb-0">
                        <li><a class="footer-link" href="/Shop#Milk"><i class="bi bi-caret-right-fill"></i> Milk</a></li>
                        <li><a class="footer-link" href="/Shop#Yogurt"><i class="bi bi-caret-right-fill"></i> Yogurt</a></li>
                        <li><a class="footer-link" href="/Shop#Cheese"><i class="bi bi-caret-right-fill"></i> Cheese</a></li>
                        <li><a class="footer-link" href="/Shop#Meat"><i class="bi bi-caret-right-fill"></i> Meat</a></li>
                        <li><a class="footer-link" href="/Shop#Cream"><i class="bi bi-caret-right-fill"></i> Creamy</a></li>
                    </ul>
                </div>

                <div class="col-6 col-lg-3">
                    <h6 class="footer-title">Support</h6>
                    <ul class="list-unstyled mb-0">
                        <li><a class="footer-link" href="/About"><i class="bi bi-info-circle"></i> About us</a></li>
                        <li><a class="footer-link" href="/About#FAQ"><i class="bi bi-question-circle"></i> FAQ</a></li>
                        <li><a class="footer-link" href="/About#RefundPolicy"><i class="bi bi-arrow-repeat"></i> Refund & Return</a></li>
                        <li><a class="footer-link" href="/About#Terms"><i class="bi bi-file-earmark-text"></i> Terms & Conditions</a></li>
                        <li><a class="footer-link" href="/About#Privacy"><i class="bi bi-shield-lock"></i> Privacy Policy</a></li>
                    </ul>
                </div>

                <div class="col-12 col-lg-3">
                    <h6 class="footer-title">Newsletter</h6>
                    <p class="mb-2">Get fresh deals & new arrivals.</p>
                    <form class="nl-wrap" method="post" action="/Subscribe">
                        <input class="nl-input" type="email" name="email" placeholder="Your email address" required />
                        <button class="nl-btn" type="submit">Subscribe</button>
                    </form>

                    <div class="social mt-3">
                        <a href="#"><i class="bi bi-facebook"></i></a>
                        <a href="#"><i class="bi bi-instagram"></i></a>
                        <a href="#"><i class="bi bi-youtube"></i></a>
                        <a href="#"><i class="bi bi-linkedin"></i></a>
                        <a href="https://github.com/sayeedsaadman/AgroFarmManagement" target="_blank"
                           rel="noopener noreferrer"
                           aria-label="SPESH AGRO on GitHub"
                           title="GitHub">
                            <i class="bi bi-github"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <style>
            .foot-bottom .human-link {
                text-decoration: none !important;
            }

            .foot-bottom .human {
                font-family: 'Montserrat', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
                font-weight: 800;
                color: #0ea5a4;
                font-size: 1.15rem;
                letter-spacing: .2px;
            }

            @@media (min-width:768px) {
                .foot-bottom .human {
                    font-size: 1.35rem;
                }
            }
        </style>

        <div class="foot-bottom py-3">
            <div class="container d-flex flex-column flex-md-row justify-content-between align-items-center gap-2 text-center text-md-start">
                <small class="copy m-0">
                    © @DateTime.Now.Year SPESH AGRO. All rights reserved.
                </small>

                <small class="m-0">
                    Made by
                    <a class="human-link" href="/About" aria-label="About SPESH AGRO">
                        <span class="human">Human</span>
                    </a>
                </small>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/tabulator-tables@5.5.0/dist/js/tabulator.min.js"></script>
    <script src="https://oss.sheetjs.com/sheetjs/xlsx.full.min.js"></script>

    @RenderSection("Scripts", required: false)

    <script>
        // Initialize AOS
        AOS.init();

        // Function to animate the numbers
        const countUp = (element, target) => {
            let start = 0;
            const duration = 2000;
            const stepTime = Math.abs(Math.floor(duration / target));
            const interval = setInterval(() => {
                start += 1;
                element.innerText = start + "+";
                if (start === target) clearInterval(interval);
            }, stepTime);
        };

        document.addEventListener('DOMContentLoaded', function () {
            const animals = document.getElementById('animals');
            const milk = document.getElementById('milk');
            const experience = document.getElementById('experience');
            const customers = document.getElementById('customers');

            const observer = new IntersectionObserver(entries => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        if (animals) countUp(animals, 50);
                        if (milk) countUp(milk, 150);
                        if (experience) countUp(experience, 20);
                        if (customers) countUp(customers, 300);
                        observer.disconnect();
                    }
                });
            }, { threshold: 0.5 });

            const stats = document.querySelector('#stats');
            if (stats) observer.observe(stats);
        });
    </script>

</body>
</html>
